import { JSONSchemaType } from "ajv";
import { Diagnostic, SourceFile } from "./types.js";
export declare class SchemaValidator<T> {
    private schema;
    private ajv;
    constructor(schema: JSONSchemaType<T>);
    /**
     * Validate the config is valid
     * @param config Configuration
     * @param file @optional file for errors tracing.
     * @returns Validation
     */
    validate(config: unknown, file: SourceFile): Diagnostic[];
}
//# sourceMappingURL=schema-validator.d.ts.map